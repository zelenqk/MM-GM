sprite_index = noone;

selected = false;

switch (type){
case 0:
	surface = surface_create_c(width, height);

	cursor = "";
	timer = cursorTimer;
	scale = ((height - 4) / string_height("a"));
	
	range = get_char_map(range)
	break;
case 1:
	if (scale = 0){
		scale = (height - paddingS / 2) / string_height("|");	
	}
	break;
}